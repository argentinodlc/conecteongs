package com.conecteongs.conecteongs.persistence.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.conecteongs.conecteongs.persistence.model.RecurrentDonation;

public interface RecurrentDonationRepository extends JpaRepository<RecurrentDonation, Long>{

}
